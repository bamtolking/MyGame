# TEST_REPORT — 가방이 무기다: 팩 앤 블래스트 (웹 베타)

작성 기준: 이 저장소의 `claude/pack-and-blast-game-5ox2gz` 브랜치 최종 커밋. 모든 결과는 실제로 실행한 것만 적었다.
**실제 휴대폰(안드로이드·iOS)에서는 확인하지 못했다.** 브라우저 검증은 헤드리스 Chromium(Playwright)의 모바일 뷰포트 에뮬레이션이다.

## 1. 실행 환경

- Node v22.22.2, npm 10.9.7, Linux 컨테이너. 의존성은 `package-lock.json` 고정 버전으로 `npm ci` 설치 (vite 7.3.1, vitest 5.0.1, typescript 5.9.3, playwright-core 1.63.0).
- 브라우저: Chromium 1194 (playwright-core), 헤드리스, `isMobile + hasTouch`, deviceScaleFactor 2.

## 2. 자동 테스트 (Vitest) — `npm run test:packblast`

결과: **6개 파일, 82개 테스트 통과, 실패 0** (기존 게임 규칙 테스트 26개 포함, 새 게임 56개).

| 파일 | 검증 항목 | 결과 |
|---|---|---|
| `tests/packblast/bag.test.ts` (12) | 12종 모두 4회 회전 = 원형, 회전 결과 정규화(0,0)·칸 수 유지, 기관총 L자 회전 좌표 검증, 초기 21칸, 가방 밖·잠긴 칸·겹침 불가 + 문제 칸 보고, L자 빈 부분에 다른 장비 배치 가능, 가장자리 회전 유효성, 이동 중 자기 칸 무시, 실패한 이동이 장비를 삭제하지 않음, 가방↔작업대 이동 중 복제 없음, 같은 종류·등급만 합성/결과 위치 유효/3등급 불가, 합성·분해 반복 시 재화 증가 없음(1+1→2, 2+2→4), 점유 칸 = 실제 모양 | 통과 |
| `tests/packblast/loadout.test.ts` (9) | 상하좌우만 인접·대각선 제외, 외접 사각형 근접은 불인정, 여러 칸 접촉 시 1회, 작업대 장비 제외, 배터리가 실제 공격 간격 변경(0.16→0.1312)·태그 불일치 시 미적용+이유, 같은 종류는 최강 하나만·다른 종류는 동시 적용, 이동 후 이전 연결 소멸·회전 반복 시 누적 없음, 탄약상자·렌즈·냉각기 태그 제한, 보호막·회복 합계(작업대 제외) | 통과 |
| `tests/packblast/run.test.ts` (14) | 무기 없으면 출발 불가, 작업대 잔여 장비 확인 없이는 출발 불가·확인 시 환급 후 출발, 출발 연타 중복 방지, 체크포인트 복원 일관성, 보상·구급팩 회복 1회만(중복 호출 무시), 전투 후 넣은 구급팩 소급 없음, 패배 결과 유지, 4구간 후 2칸 선택 해금·8구간 후 자동 해금, 보스 승리 결과, 후보 3개·같은 종류 3개 금지·생존 ≤1·최소 1개 쓸모(시드 59개), 같은 시드 = 같은 후보, 새로고침 1회·부품 부족 시 차단·후보 취소 무료 재추첨 불가, 회복 1회·6부품·20% | 통과 |
| `tests/packblast/combat.test.ts` (15) | 배터리: 3초 발사 수 +12% 이상·10초 연속 시 과열 시간 증가·배터리+냉각기는 발사·피해 모두 상승, 냉각기: 과열 시간 감소·피해 증가, 탄약상자: 추가탄 = floor(발사/4)·추가탄 재발동 없음, 렌즈: 다른 적에게 연쇄·연쇄 ≤ 발사·대상 1명이면 0·렌즈 없으면 0·드론 연쇄, 작업대 장비 무효, 죽은 적에 피해 기록 없음, 근접은 가까운 적만, 장갑은 감소하되 면역 아님, 충격파 1회·밀어냄·보호, 보스는 밀리지 않고 보호는 적용, 결정성(같은 시드 = 같은 결과)·2배속 = 스텝 수만 차이, 교착 경고→강화, 보스전 소환·압축 예고·구간 2까지 진행·종료 | 통과 |
| `tests/packblast/balance.test.ts` (4) | A/B/C 비교, 적 구성별 비교, 고정 시드 3개 × 난이도 2 전체 진행(봇) → `docs/packblast-balance.md` 생성 | 통과 |

## 3. 밸런스 실험 (실측, `docs/packblast-balance.md`)

같은 시드(101·202·303)·같은 적 구성에서 비교. 수치는 미리 정한 결론에 맞추지 않았다.

**A/B/C (기관총 단독 / +배터리 / +배터리+냉각기), 단검 동반, 보통**

| 구간 | A 발사·과열·피해 | B 발사·과열·피해 | C 발사·과열·피해 |
|---|---|---|---|
| 3 | 67 · 3.9s · 324 | 68 · 6.9s · 326 | 85 · 0.0s · 366 |
| 6 | 68 · 4.6s · 326 | 69 · 6.9s · 330 | 102 · 0.0s · 450 |

해석: 배터리만 붙이면 빨리 쏘지만 과열 정지가 길어져 총 발사 수는 거의 같다(의도한 "냉각까지 고려하게 만드는 선택"). 냉각기까지 붙이면 과열이 사라지고 발사·피해가 뚜렷이 늘어난다.

**적 구성별** (승리 수/3, 평균 남은 HP): 군집(9구간)에서는 단일 화력 빌드 3승·HP 100, 범위 빌드 3승·HP 46(폭탄 착탄 지연 동안 피해). 장갑 정예(4구간)에서는 단일 3승·HP 100, 범위 1승·HP 2. 장갑+빠름(10구간)은 1등급 고정 빌드로는 둘 다 패배(진행 중 성장한 빌드 기준이 아님).

**고정 시드 전체 진행(봇, 보통)**: 시드 303 승리(전투 시간 합계 215s), 101은 11구간 패배, 202는 8구간 패배. 전투 1회 11~27초. **어려움**: 3판 모두 4구간 패배 — 봇 기준으로 매우 어렵다(사람 플레이 후 조정 필요).

## 4. 브라우저 실기동 (Playwright, `npm run e2e:packblast`)

뷰포트 3종: 360×800(짧은 흐름), 390×844(자연 진행 전체), 430×932(전투 전 체력 보충 보조 진행으로 보스·결과 화면 확인). 스크린샷은 `e2e-out/packblast/`, 로그는 `e2e-out/packblast/report.txt`(최종), `report-run1.txt`(수정 전 1차).

실제 UI 탭으로 확인한 것 (3개 뷰포트 공통):
- 튜토리얼 새 판 생성 → 작업대 기관총 탭 → 가방 칸 탭(미리보기) → 같은 칸 재탭(확정) → 기관총이 가방 (1,0)에 배치, 튜토리얼 1→2단계.
- 배터리를 잠긴 칸(0,0)에 미리보기 → 정보 패널에 "✗ 놓을 수 없음: 잠긴 칸" 표시.
- 회전 2회 후 (2,0) 배치 → 기관총 공격 간격 0.16→0.1312초로 실제 변경, 연결 목록 `battery:true`, 튜토리얼 3단계.
- 가방 속 기관총 탭 → 정보 패널에 "✓ 배터리" 연결 표시, 튜토리얼 4단계.
- 단검·구급팩 배치 → 작업대 0.
- 정리 중 새로고침 → 이어하기 → 장비·재화·구간 상태 완전 일치.
- 출발 → 가방 닫기 전환 → 전투 화면. 일시정지 시 시간·열·적 수·투사체 수 모두 정지. 2배속 전환. **전투 중 새로고침 → 타이틀 안내 문구 + 이어하기 → 전투 시작 시점(정리 화면, 같은 구간·체력)으로 복원.**
- 전투 1 승리 → 요약 모달(무기별 피해·발사·추가탄·연쇄·과열, 받은 피해, 부품) → 보상 모달(합성 가능·지원 대상 표시) → 선택 → 작업대에 추가, 튜토리얼 5단계.
- 작업대에 장비를 남긴 채 출발 → 확인 모달(목록·환급액) → "돌아가서 정리" 시 장비 유지.
- 4구간 정예 클리어 → 해금 모드(자물쇠 2칸 탭 → 해금 확정) → 잠긴 칸 4→2. 8구간 후 0.
- 보스전: 체력바·구간 표시·압축 예고 메시지·소환, 승리/패배 요약, 결과 화면. 결과 화면에서 새로고침해도 결과 유지, 최고 도달 구간 기록.
- 도감·설정 모달 열고 닫기.
- 콘솔 오류·페이지 오류 0 (3개 뷰포트 모두).

(최종 실행 수치는 아래 "최종 실행 결과" 절에 기록한다.)

## 5. 수정한 오류 (개발 중 발견)

- 넉백 계수가 변위가 아닌 속도 기준이어서 총알 1발당 30px 밀림 → 변위 기준(총알 5px 등)으로 수정.
- 가방 캔버스가 높이만 바뀔 때(튜토리얼 문구 길이 변화) 좌표를 갱신하지 않아 탭 위치가 어긋남 → 폭·높이 모두 감지.
- 작업대 장비 선택 후 가방 칸 탭이 "선택 해제"로 처리되던 드래그/탭 원점 혼동 → 원점(칩/캔버스) 분리.
- 합성으로 사라진 장비를 가리키는 선택 상태로 렌더 오류 → 렌더 전에 해제.
- 전투 종료 처리 후 같은 프레임에서 HUD가 null 시뮬을 참조 → 종료 후 즉시 반환.
- 두 게임이 난수 모듈을 공유해 빌드 시 공용 청크가 생기고 단일 파일(play/*.html)이 깨짐(기존 게임 결과물까지 변경) → 새 게임 안에 난수 모듈 복제, 기존 게임 빌드 결과 불변 확인(`git status` 변경 없음).
- 전투 배경 레터박스, 해금 안내 문구 잘림, 무기 칩 이름 잘림 → 배경 전체 채움, 안내를 정보 패널로 이동, 칩 폭 확대.
- 보스전에서 봇이 25초 동안 128 피해를 받아 패배 → 압축 30→22, 강타 16→12로 완화(1차 e2e 이후 조정; 최종 e2e에 반영).

## 6. 확인하지 못한 항목

- 실제 휴대폰 터치·오디오·성능(중급 안드로이드, iOS Safari). 헤드리스 fps는 60이었으나 실기기 성능은 보장하지 않는다.
- 사람 플레이 기준 재미·난이도. 자동 테스트 통과는 재미 검증이 아니다.
- 가로 화면 레이아웃(최소 재배치만 구현, 스크린샷 미확인).
- 브라우저 백그라운드 복귀 대화상자(visibilitychange)는 코드로 구현했으나 헤드리스에서 탭 전환을 재현하지 않아 자동 확인하지 못했다.
- 드래그 조작은 코드로 구현했으나 e2e는 탭 흐름만 확인했다.

## 7. 알려진 제한사항

- 어려움 난이도는 봇 기준 4구간에서 전멸한다 — 보통 클리어 후 사람 플레이로 조정 필요.
- 배경음악 없음(효과음만).
- Android/iOS 앱·PWA·공개 배포 없음.
