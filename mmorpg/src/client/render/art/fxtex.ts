// Effect textures (fixed resolution, white/neutral so they can be tinted at draw time).
import { type Ctx, INK, circle, poly, line } from './core.ts';
import { Rand } from '../../audio/synth.ts';

export interface FxArt { w: number; h: number; px: number; ax?: number; ay?: number; draw: (c: Ctx) => void }
const rg = (c: Ctx, x: number, y: number, r0: number, r1: number, stops: [number, string][]) => { const g = c.createRadialGradient(x, y, r0, x, y, r1); for (const [o, col] of stops) g.addColorStop(o, col); return g; };
function inkSplat(c: Ctx, seed: number, S: number): void {
  const r = new Rand(seed); const cx = S / 2, cy = S / 2; c.fillStyle = '#fff';
  c.beginPath(); for (let i = 0; i <= 28; i++) { const a = (i / 28) * Math.PI * 2; const rr = S * (0.18 + r.next() * 0.1); const x = cx + Math.cos(a) * rr, y = cy + Math.sin(a) * rr; if (i === 0) c.moveTo(x, y); else c.lineTo(x, y); } c.fill();
  for (let i = 0; i < 16; i++) { const a = r.next() * Math.PI * 2, d = S * (0.22 + r.next() * 0.24), rr = S * (0.015 + r.next() * 0.05); circle(c, cx + Math.cos(a) * d, cy + Math.sin(a) * d, rr); c.fill(); if (r.next() < 0.5) { c.beginPath(); c.moveTo(cx + Math.cos(a) * S * 0.2, cy + Math.sin(a) * S * 0.2); c.lineTo(cx + Math.cos(a) * d, cy + Math.sin(a) * d); c.lineWidth = rr * 1.2; c.strokeStyle = '#fff'; c.stroke(); } }
}
export const FX_ART: Record<string, FxArt> = {
  soft: { w: 64, h: 64, px: 2, draw: (c) => { c.fillStyle = rg(c, 32, 32, 0, 32, [[0, 'rgba(255,255,255,1)'], [0.25, 'rgba(255,255,255,0.55)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 64, 64); } },
  light: { w: 64, h: 64, px: 1, draw: (c) => { c.fillStyle = rg(c, 32, 32, 0, 32, [[0, 'rgba(255,255,255,1)'], [0.5, 'rgba(255,255,255,0.35)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 64, 64); } },
  dot: { w: 16, h: 16, px: 2, draw: (c) => { c.fillStyle = rg(c, 8, 8, 0, 8, [[0, '#fff'], [0.45, 'rgba(255,255,255,0.9)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 16, 16); } },
  ring: { w: 128, h: 128, px: 2, draw: (c) => { circle(c, 64, 64, 58); c.strokeStyle = '#fff'; c.lineWidth = 6; c.stroke(); c.globalAlpha = 0.35; c.lineWidth = 14; c.stroke(); } },
  ringSoft: { w: 128, h: 128, px: 2, draw: (c) => { c.fillStyle = rg(c, 64, 64, 30, 64, [[0, 'rgba(255,255,255,0)'], [0.7, 'rgba(255,255,255,0.5)'], [0.86, 'rgba(255,255,255,1)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 128, 128); } },
  disc: { w: 128, h: 128, px: 2, draw: (c) => { c.fillStyle = rg(c, 64, 64, 0, 64, [[0, 'rgba(255,255,255,0.35)'], [0.85, 'rgba(255,255,255,0.55)'], [0.95, 'rgba(255,255,255,1)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 128, 128); } },
  fill: { w: 128, h: 128, px: 1, draw: (c) => { circle(c, 64, 64, 63); c.fillStyle = '#fff'; c.fill(); } },
  bar: { w: 64, h: 32, px: 2, ax: 0, ay: 0.5, draw: (c) => { const g = c.createLinearGradient(0, 0, 0, 32); g.addColorStop(0, 'rgba(255,255,255,0.9)'); g.addColorStop(0.15, 'rgba(255,255,255,0.35)'); g.addColorStop(0.85, 'rgba(255,255,255,0.35)'); g.addColorStop(1, 'rgba(255,255,255,0.9)'); c.fillStyle = g; c.fillRect(0, 0, 64, 32); } },
  beam: { w: 64, h: 16, px: 2, ax: 0, ay: 0.5, draw: (c) => { const g = c.createLinearGradient(0, 0, 0, 16); g.addColorStop(0, 'rgba(255,255,255,0)'); g.addColorStop(0.35, 'rgba(255,255,255,0.6)'); g.addColorStop(0.5, '#fff'); g.addColorStop(0.65, 'rgba(255,255,255,0.6)'); g.addColorStop(1, 'rgba(255,255,255,0)'); c.fillStyle = g; c.fillRect(0, 0, 64, 16); } },
  streak: { w: 64, h: 8, px: 2, ax: 1, ay: 0.5, draw: (c) => { const g = c.createLinearGradient(0, 0, 64, 0); g.addColorStop(0, 'rgba(255,255,255,0)'); g.addColorStop(1, '#fff'); c.fillStyle = g; c.beginPath(); c.moveTo(0, 4); c.lineTo(58, 0.5); c.quadraticCurveTo(64, 4, 58, 7.5); c.closePath(); c.fill(); } },
  spark: { w: 32, h: 8, px: 2, draw: (c) => { poly(c, [0, 4, 16, 0, 32, 4, 16, 8]); c.fillStyle = rg(c, 16, 4, 0, 16, [[0, '#fff'], [1, 'rgba(255,255,255,0.3)']]); c.fill(); } },
  star: { w: 64, h: 64, px: 2, draw: (c) => { c.fillStyle = rg(c, 32, 32, 0, 12, [[0, '#fff'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 64, 64); for (const [w, h] of [[64, 5], [5, 64]]) { const g = c.createRadialGradient(32, 32, 0, 32, 32, 32); g.addColorStop(0, '#fff'); g.addColorStop(1, 'rgba(255,255,255,0)'); c.fillStyle = g; c.beginPath(); c.ellipse(32, 32, w / 2, h / 2, 0, 0, Math.PI * 2); c.fill(); } } },
  slash: { w: 128, h: 128, px: 2, draw: (c) => { // crescent: bright leading edge, fading tail (drawn arc from -100° to +100°)
    for (let i = 0; i < 26; i++) { const t = i / 25; const a0 = -1.75 + t * 3.5, a1 = a0 + 3.5 / 25 + 0.01; const w = 2 + Math.sin(t * Math.PI) * 13; c.beginPath(); c.arc(64, 64, 52 - w / 2, a0, a1); c.strokeStyle = `rgba(255,255,255,${(0.15 + 0.85 * t).toFixed(3)})`; c.lineWidth = w; c.stroke(); }
    c.beginPath(); c.arc(64, 64, 57, -1.2, 1.7); c.strokeStyle = 'rgba(255,255,255,0.95)'; c.lineWidth = 2.2; c.stroke(); } },
  slashInk: { w: 128, h: 128, px: 2, draw: (c) => { const r = new Rand(3); for (let i = 0; i < 30; i++) { const t = i / 29; const a0 = -1.7 + t * 3.4; const w = 1 + Math.sin(t * Math.PI) * 11 * (0.8 + r.next() * 0.3); c.beginPath(); c.arc(64, 64, 50 + r.bi() * 1.5, a0, a0 + 0.13); c.strokeStyle = '#fff'; c.globalAlpha = 0.5 + 0.5 * t; c.lineWidth = w; c.stroke(); } c.globalAlpha = 1; } },
  whirl: { w: 128, h: 128, px: 2, draw: (c) => { for (let k = 0; k < 3; k++) for (let i = 0; i < 20; i++) { const t = i / 19; const a0 = k * 2.09 + t * 1.9; c.beginPath(); c.arc(64, 64, 56 - k * 4, a0, a0 + 0.12); c.strokeStyle = `rgba(255,255,255,${(t * 0.9).toFixed(2)})`; c.lineWidth = 2 + t * 9; c.stroke(); } } },
  ink0: { w: 96, h: 96, px: 1, draw: (c) => inkSplat(c, 101, 96) }, ink1: { w: 96, h: 96, px: 1, draw: (c) => inkSplat(c, 202, 96) }, ink2: { w: 96, h: 96, px: 1, draw: (c) => inkSplat(c, 303, 96) },
  smoke: { w: 64, h: 64, px: 1, draw: (c) => { const r = new Rand(9); for (let i = 0; i < 9; i++) { const x = 32 + r.bi() * 12, y = 32 + r.bi() * 12, rr = 10 + r.next() * 12; c.fillStyle = rg(c, x, y, 0, rr, [[0, 'rgba(255,255,255,0.5)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 64, 64); } } },
  flame: { w: 32, h: 48, px: 2, draw: (c) => { const g = c.createRadialGradient(16, 34, 1, 16, 30, 20); g.addColorStop(0, '#fff'); g.addColorStop(0.5, 'rgba(255,255,255,0.8)'); g.addColorStop(1, 'rgba(255,255,255,0)'); c.fillStyle = g; c.beginPath(); c.moveTo(16, 2); c.bezierCurveTo(24, 16, 30, 26, 27, 36); c.arc(16, 36, 11, 0, Math.PI); c.bezierCurveTo(2, 26, 9, 16, 16, 2); c.fill(); } },
  paper: { w: 14, h: 20, px: 3, draw: (c) => { c.fillStyle = '#ffe07a'; c.fillRect(1, 1, 12, 18); c.strokeStyle = '#9a7a2a'; c.lineWidth = 1; c.strokeRect(1, 1, 12, 18); c.fillStyle = '#c8323a'; c.font = 'bold 10px serif'; c.textAlign = 'center'; c.textBaseline = 'middle'; c.fillText('符', 7, 10.5); } },
  arrow: { w: 40, h: 8, px: 3, ax: 1, ay: 0.5, draw: (c) => { line(c, [4, 4, 34, 4], '#f0e6cc', 1.6); poly(c, [40, 4, 33, 1, 33, 7]); c.fillStyle = '#dfe8f4'; c.fill(); poly(c, [0, 1, 7, 4, 0, 7, 3, 4]); c.fillStyle = '#e0344d'; c.fill(); } },
  coin: { w: 16, h: 16, px: 3, draw: (c) => { circle(c, 8, 8, 7); const g = c.createRadialGradient(6, 6, 1, 8, 8, 8); g.addColorStop(0, '#fff6c0'); g.addColorStop(1, '#d8a020'); c.fillStyle = g; c.fill(); c.strokeStyle = '#7a5410'; c.lineWidth = 1.2; c.stroke(); c.fillStyle = '#7a5410'; c.fillRect(5.8, 5.8, 4.4, 4.4); c.fillStyle = 'rgba(0,0,0,0)'; } },
  feather: { w: 24, h: 8, px: 3, draw: (c) => { c.beginPath(); c.ellipse(12, 4, 11, 3.2, 0, 0, Math.PI * 2); c.fillStyle = '#26223a'; c.fill(); line(c, [1, 4, 23, 4], '#8a7aa8', 0.8); } },
  petal: { w: 12, h: 12, px: 3, draw: (c) => { c.beginPath(); c.moveTo(6, 1); c.quadraticCurveTo(12, 6, 6, 11); c.quadraticCurveTo(0, 6, 6, 1); c.fillStyle = '#fff'; c.fill(); } },
  leaf: { w: 14, h: 10, px: 3, draw: (c) => { c.beginPath(); c.moveTo(1, 5); c.quadraticCurveTo(7, -1, 13, 5); c.quadraticCurveTo(7, 11, 1, 5); c.fillStyle = '#fff'; c.fill(); line(c, [1, 5, 13, 5], 'rgba(0,0,0,0.3)', 0.7); } },
  shard: { w: 18, h: 8, px: 3, draw: (c) => { poly(c, [0, 4, 6, 0, 18, 4, 6, 8]); c.fillStyle = '#fff'; c.fill(); poly(c, [6, 0, 18, 4, 6, 4]); c.fillStyle = 'rgba(160,220,255,0.6)'; c.fill(); } },
  note: { w: 12, h: 16, px: 3, draw: (c) => { circle(c, 4, 12, 3.2); c.fillStyle = '#fff'; c.fill(); c.fillRect(6.2, 2, 1.6, 10); c.beginPath(); c.moveTo(7.8, 2); c.quadraticCurveTo(12, 4, 10, 8); c.lineWidth = 1.6; c.strokeStyle = '#fff'; c.stroke(); } },
  pillar: { w: 32, h: 128, px: 2, ax: 0.5, ay: 1, draw: (c) => { const g = c.createLinearGradient(0, 0, 0, 128); g.addColorStop(0, 'rgba(255,255,255,0)'); g.addColorStop(1, 'rgba(255,255,255,1)'); c.fillStyle = g; const h = c.createLinearGradient(0, 0, 32, 0); h.addColorStop(0, 'rgba(0,0,0,1)'); h.addColorStop(0.5, 'rgba(0,0,0,0)'); h.addColorStop(1, 'rgba(0,0,0,1)'); c.fillRect(0, 0, 32, 128); c.globalCompositeOperation = 'destination-out'; c.fillStyle = h; c.fillRect(0, 0, 32, 128); } },
  seal: { w: 64, h: 64, px: 2, draw: (c) => { c.save(); c.translate(32, 32); c.rotate(-0.08); c.fillStyle = '#d0213a'; c.beginPath(); c.roundRect(-28, -28, 56, 56, 6); c.fill(); c.strokeStyle = '#f7e8e8'; c.lineWidth = 3; c.strokeRect(-23, -23, 46, 46);
    c.fillStyle = '#f7e8e8'; c.font = 'bold 22px serif'; c.textAlign = 'center'; c.textBaseline = 'middle'; c.fillText('退', -10, -9); c.fillText('魔', 10, -9); c.fillText('封', -10, 12); c.fillText('印', 10, 12); c.restore(); } },
  sigil: { w: 256, h: 256, px: 2, draw: (c) => { c.translate(128, 128); c.strokeStyle = '#fff'; c.fillStyle = '#fff';
    for (const [r, w] of [[124, 3], [112, 1.5], [70, 2], [60, 1]]) { c.beginPath(); c.arc(0, 0, r, 0, Math.PI * 2); c.lineWidth = w; c.stroke(); }
    const tri = ['☰', '☱', '☲', '☳', '☴', '☵', '☶', '☷']; c.font = 'bold 22px serif'; c.textAlign = 'center'; c.textBaseline = 'middle';
    tri.forEach((t, i) => { c.save(); c.rotate((i / 8) * Math.PI * 2); c.fillText(t, 0, -91); c.restore(); });
    const words = '急急如律令退魔除災'; c.font = 'bold 13px serif'; for (let i = 0; i < words.length * 2; i++) { c.save(); c.rotate((i / (words.length * 2)) * Math.PI * 2 + 0.2); c.fillText(words[i % words.length], 0, -118); c.restore(); }
    for (let i = 0; i < 8; i++) { const a = (i / 8) * Math.PI * 2; c.beginPath(); c.moveTo(Math.cos(a) * 60, Math.sin(a) * 60); c.lineTo(Math.cos(a + Math.PI * 3 / 8 * 2) * 60, Math.sin(a + Math.PI * 3 / 8 * 2) * 60); c.lineWidth = 1.5; c.stroke(); }
    c.beginPath(); c.arc(0, 0, 26, 0, Math.PI * 2); c.lineWidth = 2; c.stroke(); c.beginPath(); c.arc(0, -13, 13, -Math.PI / 2, Math.PI / 2); c.arc(0, 13, 13, -Math.PI / 2, Math.PI / 2, true); c.arc(0, 0, 26, Math.PI / 2, -Math.PI / 2); c.fill(); } },
  runes: { w: 128, h: 128, px: 2, draw: (c) => { c.translate(64, 64); c.strokeStyle = '#fff'; c.fillStyle = '#fff'; c.beginPath(); c.arc(0, 0, 60, 0, Math.PI * 2); c.lineWidth = 2; c.stroke(); c.beginPath(); c.arc(0, 0, 50, 0, Math.PI * 2); c.lineWidth = 1; c.stroke();
    const g = '火水木金土日月山'; c.font = 'bold 11px serif'; c.textAlign = 'center'; c.textBaseline = 'middle'; for (let i = 0; i < 16; i++) { c.save(); c.rotate((i / 16) * Math.PI * 2); c.fillText(g[i % g.length], 0, -55); c.restore(); } } },
  hex: { w: 96, h: 96, px: 2, draw: (c) => { c.translate(48, 48); c.strokeStyle = '#fff'; for (let ring = 0; ring < 3; ring++) for (let i = 0; i < 6; i++) { const a = (i / 6) * Math.PI * 2 + ring * 0.52, r = 14 + ring * 14; c.beginPath(); for (let k = 0; k <= 6; k++) { const b = (k / 6) * Math.PI * 2; const x = Math.cos(a) * r + Math.cos(b) * 7, y = Math.sin(a) * r + Math.sin(b) * 7; if (k === 0) c.moveTo(x, y); else c.lineTo(x, y); } c.lineWidth = 1.4; c.globalAlpha = 0.9 - ring * 0.25; c.stroke(); } } },
  crack: { w: 128, h: 128, px: 1, draw: (c) => { const r = new Rand(77); c.translate(64, 64); c.strokeStyle = '#fff'; c.lineCap = 'round';
    for (let i = 0; i < 9; i++) { let a = (i / 9) * Math.PI * 2 + r.bi() * 0.3, x = 0, y = 0, w = 4.5; c.beginPath(); c.moveTo(0, 0); for (let s = 0; s < 6; s++) { a += r.bi() * 0.5; const L = 7 + r.next() * 6; x += Math.cos(a) * L; y += Math.sin(a) * L; c.lineTo(x, y); } c.lineWidth = w; c.stroke(); } } },
  scorch: { w: 64, h: 64, px: 1, draw: (c) => { c.fillStyle = rg(c, 32, 32, 2, 30, [[0, 'rgba(255,255,255,0.95)'], [0.6, 'rgba(255,255,255,0.55)'], [1, 'rgba(255,255,255,0)']]); c.fillRect(0, 0, 64, 64); } },
  danger: { w: 32, h: 32, px: 3, draw: (c) => { c.fillStyle = '#fff'; c.font = 'bold 26px serif'; c.textAlign = 'center'; c.textBaseline = 'middle'; c.fillText('危', 16, 17); } },
  eye: { w: 8, h: 8, px: 2, draw: (c) => { circle(c, 4, 4, 3.5); c.fillStyle = '#fff'; c.fill(); } },
};
export { INK };
