# TEST_REPORT — 갈아타!: 바디 하이재킹 0.1.0-beta.1

검증 환경: Linux 컨테이너, Node 22, Vitest 5, Playwright-core 1.63 + 사전 설치 Chromium(헤드리스). **실기기(안드로이드/iOS)와 사람 플레이는 수행하지 않았습니다.**

## 1. 자동 테스트 (`npm test`) — 38개 통과, 실패 0

원본 로그: `docs/test-run.log`

### 빙의 (tests/possession.test.ts, 11개)
| 항목 | 결과 |
|---|---|
| 체력 35% 이하일 때만 빙의 가능(36%는 불가) | 통과 |
| 기절 상태면 체력과 무관하게 빙의 가능 | 통과 |
| 보스·포탑·노드는 체력 1이어도 빙의 불가 | 통과 |
| 빙의 불가 적에게 버튼을 눌러도 몸이 바뀌지 않고 이유가 안내됨("아직 약하지 않다", "코어 잠금") | 통과 |
| 여러 후보 중 반지름을 뺀 거리 기준 가장 가까운 대상, 반복 호출 시 일관 | 통과 |
| 빙의 성공 시 몸 교체, 이전 몸 제거, 체력 유지(최소 45% 회복), 안정도 새 몸 최대치, 무기(산탄 6발)·스킬(방어 자세) 변경 | 통과 |
| 남은 체력이 45%보다 높으면 그대로 이어받음 | 통과 |
| 빙의 직후 1.75초 재사용 대기, 이후 가능 | 통과 |
| 안정도 시간 감소(정찰병 초당 2.2), 피해 시 추가 감소, 0 → 붕괴 → 체력 감소, 붕괴 중 빙의로 생존 | 통과 |
| 체력 0 + 근처 후보 → 마지막 기회(슬로모션) → 빙의로 생존 / 후보 없음 → 즉시 패배 / 시간 초과 → 패배 | 통과 |
| 빙의한 몸의 투사체는 플레이어 편이 되고, 적 AI는 새 몸을 표적으로 삼음 | 통과 |

### 전투 (tests/combat.test.ts, 12개)
| 항목 | 결과 |
|---|---|
| 죽은 적에게 추가 피해·중복 처치 없음 | 통과 |
| 피격 무적 0.3초 동안 다른 사격 무시, 같은 볼리(산탄)는 상한 25%까지, 한 스텝 40% 상한 | 통과 |
| 직선 탄은 벽에 막힘, 폭탄은 벽을 넘어 금 간 벽(50)을 2발에 파괴 | 통과 |
| 과부하 폭탄은 벽을 1발에 파괴하고 자신에게도 피해(치명적이지는 않음) | 통과 |
| 저격 관통탄이 적 2명을 관통하고 원거리 스위치를 켜 문 B 개방 | 통과 |
| 침입자 사거리(230)로는 스위치(256+)에 닿지 않음 | 통과 |
| 방패병: 정면 포탑 탄 차단, 옆(51° 밖)은 피격, 방어 자세 중 옆도 차단, 뒤는 피격 | 통과 |
| 적 방패병은 정면 사격을 막으므로 뒤에서 쏴야 약화됨 | 통과 |
| 폭발은 방패로 막히지 않음 | 통과 |
| 정비병 전기 아크 → 감전 누적 → 기절 → 체력과 무관하게 빙의 후보 / 수복 펄스 체력 22·안정도 12 회복 | 통과 |
| 6개 몸의 무기 종류·이동 속도·스킬이 모두 다름(집합 크기 6) | 통과 |
| 각 몸으로 실제 공격 시 서로 다른 투사체/이벤트 발생 | 통과 |

### 구역 (tests/zones.test.ts, 15개) — 헤드리스 봇이 실제로 플레이
| 구역 | 시나리오 | 결과 |
|---|---|---|
| 1 | **B안** 정찰병 약화→빙의→(통로 안 방패병 유인)→약화→빙의→포탑 통로→패널→문 A→출구 | **통과** 20.3초, 피해 32, 포탑 탄 차단 7회, 사슬 침입자>정찰병>방패병 |
| 1 | **B2안** 정찰병 처치→폭탄병 약화→빙의→왼쪽 통로→금 간 벽 파괴→출구(방패병은 건드리지 않음) | **통과** 17.5초, 피해 15 |
| 1 | **A안 비교** 기본 몸으로 전부 처치 시도 | **패배** 17.3초(정찰병·폭탄병 처치 후 방패병 상대 중 사망). → 남겨두고 갈아타는 B안이 더 안전하고 실제로 통과함 |
| 2 | 방패병 정면 돌파(입구 방 적은 대기 상태) | 통과 9.1초, 통로 피해 31, 차단 4회 |
| 2 | 폭탄병으로 입장→달려드는 적 정리→방패병 폭격 약화→빙의→통로 | 통과 21.5초(체력 1 남음, 빠듯함) |
| 2 | 정찰병 질주 강행(포탑만) | 3/3 통과, 피해 8(포탑 회전 조정 후) |
| 2 | 정비병 과부하 단자 → 포탑 4기 정지 → 무피해 통과 | 통과 |
| 3 | 폭탄병 벽 파괴 → 출구 | 통과 11.4초 |
| 3 | 저격병 구덩이 건너 스위치 → 문 B → 출구 | 통과 16.1초 |
| 3 | 침입자만으로는 벽·스위치 모두 반응 없음(몸이 필요한 구조) | 통과 |
| 3 | 자동 정책: 방패병으로 입장, 폭탄병·저격병은 남겨두고 싸우다 그중 하나 빼앗기 | 3판 중 2판 성공(폭탄병 탈취) |
| 4 | 구조: 웨이브 3차 순서대로 소환, 3차 후 경비 1명 이하 → 문 C 개방 → 출구 | 통과 |
| 4 | 자동 정책 봇 생존 기록(참고) | 5판 중 1판 클리어(사슬 침입자>폭탄병>정비병>저격병, 3회 갈아탐). 나머지는 2차 경비에서 사망 |
| 5 | 구조: 보스 빙의 불가, 66%에서 보호막·노드 2·포탑 2·소환, 보호막 중 피해 0, 노드까지 걸어갈 경로 존재, 노드 파괴 → 보호막 해제, 33%에서 3단계(포탑 정지·돌진·정비병 소환), 처치 시 승리 | 통과 |
| 5 | 자동 정책 봇 보스전 기록(참고) | **5판 중 0승**. 모두 1단계는 넘기고(코어 900→588) 2단계 노드 구간에서 사망 |

## 2. 실기동 e2e (`npm run e2e`) — 3개 뷰포트 통과, 콘솔 오류 0

원본 로그: `docs/e2e-report.txt`, 스크린샷: `docs/screenshots/`

| 항목 | 360×800 | 390×844 | 430×932 |
|---|---|---|---|
| 타이틀 → "새 침입 시작" → 게임 화면 | 통과 | 통과 | 통과 |
| 실제 터치 드래그(조이스틱)로 이동, 손 떼면 정지 | dy −134, 정지 | dy −134, 정지 | dy −133, 정지 |
| 공격 버튼 누르고 있기 → 연사(0.4초에 8~9발), 떼면 중단 | 통과 | 통과 | 통과 |
| 자동 조작으로 정찰병 약화→빙의 가능 표시→빙의 성공, HUD 몸 이름/스킬 갱신 | 통과 | 통과 | 통과 |
| 일시정지 시 시간 정지, 계속하기 시 진행 | 통과 | 통과 | 통과 |
| 도움말·설정 열고 닫기 | 통과 | 통과 | 통과 |
| 새로고침 후 "이어하기" 표시, 구역 시작 지점부터 재개 | 통과 | 통과 | 통과 |
| 버튼 겹침 없음, 화면 안, 최소 크기 40px | 통과 | 통과 | 통과 |

## 3. 재미 검증용 비교 (A 전부 처치 vs B 남겨두고 갈아타기)

1구역 동일 조건 봇 실험:
- A(전부 즉시 처치, 침입자 유지): 정찰병·폭탄병은 처치했으나 방패병 앞에서 사망(17.3초). 폭탄병을 죽였으므로 지름길(금 간 벽)도 사라짐.
- B(정찰병→방패병 갈아타기): 20.3초에 통과, 포탑 탄 7회 차단.
- B2(폭탄병만 빼앗아 벽 파괴): 17.5초에 통과, 받은 피해 15로 가장 안전하고 가장 빠름.

→ "몸으로 쓰기 위해 남겨두는 판단"이 **더 안전하거나(B2 피해 15 vs A 사망)**, **다른 경로를 열거나(벽 파괴)** 하는 형태로 실제 보상됨.

## 4. 남은 문제 · 미확인 항목

- **사람 플레이 미수행.** 다음 질문은 휴대폰에서 직접 확인이 필요합니다: 빙의 가능한 적이 바로 눈에 들어오는가 / 새 몸의 느낌이 확실히 다른가 / 일부러 남겨두고 싶은 적이 생기는가 / 위기 빙의가 짜릿한가 / 상황별 선택이 생기는가 / 다른 조합을 써보고 싶은가.
- **보스 2단계 난이도 미검증**: 자동 봇은 0/5. 구조(노드 파괴로 보호막 해제, 노드까지 경로)는 검증했고, 보호막 중 코어 공격 정지로 완화했지만, 사람이 넘을 수 있는 난이도인지는 실기기 플레이로 확인해야 합니다. 어렵다면 `src/data/bodies.ts` 보스 hp(900), `src/sim/boss.ts` 소환 구성·`node` 체력(70)을 낮추는 것을 권장.
- **4구역 웨이브 난이도**: 봇 1/5 클리어. 봇의 전술 수준이 낮아(측면 공격·후퇴가 서툼) 사람 기준 판단은 보류.
- **밸런스 편차**: 2구역 "폭탄병→방패병" 시나리오는 체력 1로 통과(빠듯). 적 피해 배율은 `rules.ts`의 `enemyDamageMul`(0.55)로 한 번에 조정 가능.
- 실기기 터치·오디오·성능(FPS) 미확인. 헤드리스 Chromium에서는 오류 없음.
- 가로 화면은 레이아웃만 대응(조작 영역 비율 조정), 실제 검증 없음.
- 저장 손상 시 기본값 복구 로직은 코드로 구현했으나 손상 데이터 주입 테스트는 하지 않았습니다.
