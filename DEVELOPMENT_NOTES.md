# DEVELOPMENT_NOTES — 가방이 무기다: 팩 앤 블래스트

이 문서는 새 게임(팩 앤 블래스트)의 구조·규칙·수치 위치·완료/미완료 항목을 정리한다. 같은 저장소의 기존 게임(대박수비대)은 `docs/STATUS.md`, `docs/BALANCE.md`를 보라.

## 1. 프로젝트 구조

두 게임이 하나의 Vite 프로젝트를 공유한다 (멀티페이지). 기존 게임의 파일은 건드리지 않았다.

```
index.html                 기존 게임 진입 (/)
packblast/index.html       새 게임 진입 (/packblast/)   ← vite.config.ts rollupOptions.input
src/packblast/
  data/equipment.ts        장비 12종: 모양(상대 좌표), 태그, 등급별 수치, 설명, 과열 스펙
  data/enemies.ts          적 5종 + 보스, 장갑 규칙(ARMOR_MIN_RATIO), 구간별 체력·피해 배율, 보스 패턴 수치
  data/waves.ts            12구간 등장 계획(STAGES_NORMAL), 어려움 변형(getStages), 위협 문구
  data/balance.ts          경제(부품·회복·새로고침·분해 환급), 가방 크기·해금, 충격파, 교착 안전장치, 고정 스텝, 보상 가중치
  core/shapes.ts           회전(90° 시계방향)+정규화, 비교
  core/bag.ts              격자·잠금·점유 계산, checkPlacement(칸별 문제 이유), validateAll
  core/loadout.ts          computeAdjacency(변 공유만, 쌍 중복 없음), computeLoadout(지원 효과 → 무기 설정), weaponStatLines
  core/rewards.ts          후보 3개 생성 규칙, candidateFlags(합성 가능·지원 대상)
  core/run.ts              RunState와 모든 확정 행동 (배치·작업대·합성·분해·회복·보상·해금·출발·결과 반영·체크포인트)
  core/save.ts             localStorage 저장·검증·복원, 전투 중 이탈 시 체크포인트 복귀, 초기화
  combat/types.ts          전투 통계 타입
  combat/sim.ts            BattleSim: 고정 스텝(1/60s) 결정적 전투. 무기 6종, 열, 탄약상자 추가탄, 렌즈 연쇄, 적 5종, 보스, 충격파, 교착 강화, 이벤트 큐
  render/sprites.ts        절차적 그래픽: 장비 본체/아이콘, 캐릭터(대기·피격·반동·승리·사망), 적 5종, 보스(구간별 외형)
  render/bagView.ts        가방 캔버스(프레임·안감·자물쇠), 배치 미리보기, 연결 표시(적용 ✓/미적용 ×), 착지·회전·합성 연출
  render/battleView.ts     전투 캔버스: 이벤트→연출(파티클 상한 160, FX 40), 흔들림, 보스 체력바, 경고 배너
  ui/app.ts                화면(타이틀·정리·전투·결과), 입력(탭·드래그), 게임 루프, 보상·요약·해금 모달, 설정·도감·도움말, 튜토리얼
  ui/modal.ts, ui/dom.ts   모달·토스트·DOM 헬퍼
  platform/audio.ts        절차적 WebAudio 효과음(동시 재생 제한, 이름별 최소 간격)
  style.css, main.ts       스타일(세로 우선, 안전 영역, 700px 이하·가로 대응), 진입
tests/packblast/
  bag.test.ts              회전·배치·이동·합성·분해 규칙
  loadout.test.ts          인접·지원 효과 규칙
  run.test.ts              출발 검사·체크포인트·결과 반영·해금·보상·경제
  combat.test.ts           배터리/냉각기/탄약상자/렌즈 실제 효과, 판정, 충격파, 보스, 결정성, 교착
  balance.test.ts + bot.ts A/B/C 비교, 적 구성별 비교, 고정 시드 전체 진행 봇 → docs/packblast-balance.md
scripts/e2e-packblast.mjs  Playwright(Chromium) 휴대폰 뷰포트 3종 실기동 (스크린샷 e2e-out/packblast/)
scripts/singlefile-packblast.mjs  dist/packblast → play/packblast.html
```

의존성 추가 없음(기존 vite/vitest/typescript/playwright-core 재사용). Phaser는 쓰지 않았다: 기존 프로젝트가 Canvas 2D 구조였고, 추가 번들 없이 같은 결과를 낼 수 있어서다.

## 2. 주요 게임 규칙 (구현 기준)

- 한 판 = 12구간. 1~3 기본, 4 정예, 5~7 강화 기본, 8 정예, 9~11 후반, 12 보스. 체력은 전투 사이에 이어지고 자동 회복 없음.
- 흐름: 정리(prep) → 출발 → 전투(battle) → 결과 요약 → [4구간 후 해금(unlock)] → 보상(reward) → 정리 …  보스 승리/체력 0 → 결과(result).
- 가방 5×5, 모서리 4칸 잠금(21칸). 4구간 클리어 후 2칸 선택 해금, 8구간 클리어 후 나머지 자동 해금. 최대 25칸.
- 배치는 실제 점유 칸 기준. 가방 밖·잠긴 칸·다른 장비 위 불가. 회전은 좌표 변환 후 정규화(4번 회전 = 원형). 좌우 반전 없음.
- 인접 = 실제 점유 칸이 상하좌우로 변을 공유. 대각선 제외. 같은 쌍은 한 번만. 작업대 장비 제외. 배치 변경 때마다 전체를 새로 계산(누적 없음).
- 같은 종류 지원은 가장 강한 하나만(등급 높은 것), 다른 종류는 동시 적용. 태그 불일치·중복은 이유와 함께 표시.
- 과열: 기관총·레이저만. 발사당 열 증가, 시간당 방출, max 도달 시 정지, max×resumeAt(40%) 이하가 되면 재개. 냉각기는 방출률 배율. 배터리는 간격을 줄여 열 누적도 빨라진다.
- 탄약상자: 원본 공격 N회마다 추가 탄 1회(기관총 추가 탄환 / 산탄총 +2탄 강화 산탄 / 폭탄 50% 미니 폭탄). 추가 탄은 카운트에 포함되지 않아 재발동 없음. 산탄 1발 = 원본 1회.
- 공명 렌즈: 원본 공격 1회당 최대 1회, 원래 적·이미 맞은 적 제외 90px 내 가장 가까운 다른 적에게 감소 피해. 연쇄는 새 연쇄를 만들지 않음. 관통 레이저는 광선 1회당 1회.
- 방패판: 전투 시작 시 가방 합계만큼 보호막(재충전 없음). 구급팩: 전투 시작 시 확정된 합계로 클리어 후 회복(전투 후 넣어도 소급 없음). 둘 다 `run.battle`에 시작 시 확정.
- 전투 종료: 예정 적 모두 등장 + 모두 처치. 보스전은 보스 처치. 교착: 마지막 등장 후 45s 경고, 55s 강화(피해 ×1.4, 속도 ×1.25, 이후 15s마다 반복). 보스전은 ×2.2.
- 긴급 충격파: 전투 1회. 반경 130 적 95px 밀기(보스는 저항), 1.6s 보호, 피해 3.
- 적: 기본형, 질주형(빠름·약함), 군집형(작고 다수), 장갑형(피격당 4 감소, 최소 30%), 자폭형(28px 접근 후 0.9s 예고 → 반경 52 폭발 22). 보스 "거대 고철 수거기": 접근·강타, 부하 소환(단계별 구성), 1.5s 예고 압축 공격 30(보호막·충격파로 대응), 체력 66%/33%에서 장갑판 탈락(피해 +15/30%, 속도 +25/50%, 1s 경직).
- 경제: 부품 시작 4, 일반 +4, 정예 +8, 새로고침 3(보상마다 1회), 회복 6(정비마다 1회, 최대 체력 20%), 분해 환급 1/2/4. 합성은 같은 종류·등급 2개 → +1등급(최대 3), 모양 유지, 가방 장비 위치 유지.
- 보상 후보: 같은 종류 3개 금지, 생존 최대 1, 지원 최대 2, 최소 1개는 쓸모(무기·합성 가능·지원 대상 있음). 2등급 확률은 구간에 따라 0/10/20/35%.
- 난이도: 보통, 어려움(보통 클리어 후 해금). 어려움은 등장 수 ×1.2, 간격 ×0.85, 3구간부터 자폭 1 추가, 정예 장갑 +1, 6구간 이후 질주 +2, 보스 호위·소환 간격 ×0.75·압축 9s, 체력·피해 ×1.1.
- 영구 진행: 최고 도달 구간, 클리어 수, 어려움 해금, 발견한 인접 효과, 장비 도감, 최근 기록 20개.
- 저장: 확정 행동마다 즉시 저장(`App.persist`). 출발 시 정리 상태를 `checkpoint`(JSON)로 저장하고 phase='battle'. 결과 반영(`applyBattleResult`)은 phase가 battle일 때만 1회. 로드 시 phase가 battle이면 체크포인트로 복귀하고 안내. 손상 데이터는 `validateRun`으로 검사해 판만 초기화하고 안내.

## 3. 밸런스 수치 위치

| 항목 | 파일 |
|---|---|
| 장비 피해·간격·사거리·등급별 부가 수치, 과열(max/perShot/coolRate/resumeAt), 지원 값 | `src/packblast/data/equipment.ts` |
| 적 체력·속도·피해·장갑·자폭, 보스 소환/압축/구간 배율, 구간별 체력·피해 배율 | `src/packblast/data/enemies.ts` |
| 구간별 등장 시각·수·방향, 위협 문구, 어려움 변형 | `src/packblast/data/waves.ts` |
| 부품·회복·새로고침·분해 환급, 해금 시점, 충격파, 교착 시간, 보상 가중치·2등급 확률 | `src/packblast/data/balance.ts` |

수치를 바꾼 뒤 `npm run balance:packblast`로 `docs/packblast-balance.md`를 다시 만들어 비교하라.

## 4. 구현 완료 / 미완료

완료(자동 테스트 + 헤드리스 Chromium에서 확인):
- 1단계~4단계 항목 전부: 가방 격자·회전·배치·작업대·합성·분해, 장비 12종, 지원 4종의 실제 전투 반영, 과열, 무기별 다른 판정, 연결 표시, 12구간·적 5종·정예·보스·가방 확장·난이도·경제·회복·충격파, 화면 크기별 UI, 튜토리얼(5단계·건너뛰기·다시 보기), 조작 연출(들림·미리보기·착지·회전·합성 링·가방 닫기 전환), 타격감(반동·궤적·피격·넉백·폭발·파티클 상한·효과음), 흔들림/효과 감소 설정, 전투 요약(실측치), 일시정지(모두 정지)·백그라운드 자동 정지·복귀 확인, 저장·이어하기·체크포인트·초기화, 도감·기록.

미완료 / 제한:
- 실기기(안드로이드 Chrome·iOS Safari) 확인은 하지 못했다. 헤드리스 Chromium 모바일 에뮬레이션(360×800, 390×844, 430×932)만 확인.
- 배경음악 없음(효과음만). 음량·음소거 설정은 있음.
- 드래그는 보조 조작이며 작업대 칩 → 가방 드래그는 캔버스 좌표 기준으로만 동작(작업대 밖으로 드래그해 빼기는 없음 — "작업대로" 버튼 사용).
- 가로 화면은 최소한의 재배치만 했다(세로 우선).
- Android/iOS 앱 패키징은 하지 않았다. `src/packblast/platform/`과 순수 로직 분리로 Capacitor 등으로 감쌀 수 있는 구조.
- PWA(오프라인 설치) 없음. 공개 배포 없음.

## 5. 이어서 작업할 때 실행 명령

```bash
npm install            # 최초 1회
npm run dev            # http://localhost:5173/packblast/  (휴대폰: http://<PC IP>:5173/packblast/)
npm run typecheck
npm run test:packblast
npm run e2e:packblast  # dev 서버 켠 상태에서
npm run build          # dist/ + play/packblast.html
```

## 6. 남아 있는 문제·조정 후보

- 밸런스는 봇 기준으로만 검증했다(`docs/packblast-balance.md`). 봇은 방패판을 잘 고르지 않고 단순 배치를 하므로 사람 기준 난이도는 다를 수 있다. 사람 플레이 3~5판 후 `STAGE_HP_MUL`, 보스 `hp`, `crushDamage`, 과열 `coolRate`부터 조정하라.
- 봇 결과에서 기관총 단독 과열 정지 시간이 길게 나오는 판이 있다(냉각기를 못 얻은 경우). 답답하면 `equipment.ts`의 mg `coolRate`를 올리거나 `resumeAt`을 높여라.
- 보스전 목표 45~60초에 맞추기 위해 보스 체력을 4200으로 올렸다. 강한 빌드는 더 빨리, 약한 빌드는 압축 공격 3회 이상을 견뎌야 한다. 실제 플레이 후 조정.
- 어려움은 봇이 4구간에서 전멸한다. 보통 클리어 후 사람이 해 보고 등장 수 배율(1.2)을 조정하라.
- 자동 정리 기능은 의도적으로 없다.
