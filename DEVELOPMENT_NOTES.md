# DEVELOPMENT_NOTES — 합체방어대: 콤보 러시

## 1. 현재 구조

```
game/
  index.html            진입 HTML (Vite root = game/)
  src/main.ts           App 생성, window.__game 테스트 훅
  src/style.css         모바일 세로 우선 레이아웃, 안전 영역, 가로 화면 재배치
  src/data/             ★ 모든 밸런스 수치 (여기만 고치면 됨)
    units.ts            유닛 8종 기본 수치, 등급 배율(GRADE_MUL), 등급별 세부(UNIT_PARAMS), 상태 효과(STATUS), 투입 골드(GRADE_INVEST)
    enemies.ts          적 5종·보스 2종 체력/속도/보상/장갑, 드론(DRONE), 보스 패턴(BOSS)
    waves.ts            18웨이브 구성(WAVES_NORMAL), 어려움 추가 그룹(HARD_EXTRA), 난이도 배율(DIFF), 체력 곡선(hpMul)
    maps.ts             맵 2개 경로·배치 칸·기지, 경로 기하 함수
    economy.ts          시작 골드, 소환/새로고침 비용, 판매 환급률, 준비 시간, 웨이브 보상
    combos.ts           연계 6종 정의·수치(COMBO_NUM)
  src/sim/              순수 로직 (DOM/Phaser 없음, 전부 테스트 가능)
    rng.ts              sfc32 시드 난수(상태가 데이터라 저장·복원 가능)
    types.ts            GameState / Unit / Enemy / Projectile / SimEvent
    state.ts            newGame, 유닛·적 생성, 로그
    offer.ts            소환 후보 3개 생성(서로 다른 종류, 첫 후보 공격 유닛 보장, 튜토리얼 고정 후보)
    roster.ts           배치·새로고침·이동·합성·판매 (검증 후 원자적 변경)
    combat.ts           타깃 선택, 유닛별 공격, 투사체, 피해·상태 효과, 적 이동, 드론·보스 패턴
    combos.ts           연계 6종 판정 (combo=true 피해는 재발동 금지)
    engine.ts           웨이브 진행·승패·step()·dispatch()·serialize/deserialize
  src/render/           Canvas 2D
    sprites.ts          절차적 스프라이트(유닛 8×3등급, 적 7종) + 캐시
    fx.ts               파티클·링·광선·부채꼴·회오리·라벨, 병합 피해 숫자
    renderer.ts         배경 캐시(맵 테마), 적/유닛/투사체/연출, 이벤트 소비
  src/ui/app.ts         화면(제목/선택/게임/결과), HUD, 후보 카드, 유닛 메뉴, 입력, 메인 루프, 일시정지, 저장 연결, 튜토리얼, 모달
  src/platform/
    storage.ts          메타·체크포인트 저장(버전·손상 처리·메모리 폴백)
    audio.ts            절차적 WebAudio 효과음(사용자 입력 후 시작, 중첩 제한)
  tests/                Vitest: rules / combos / save / balance(봇) + helpers, bot
  scripts/e2e.mjs       Playwright(Chromium) 휴대폰 뷰포트 e2e
  scripts/singlefile.mjs 단일 파일 빌드
  docs/balance-results.md  봇 시뮬레이션 결과(npm run balance 가 갱신)
```

루트: `package.json`, `vite.config.ts`(root=game), `tsconfig.json`, `vite.legacy.config.ts`(이전 게임용). 이전 게임 파일(`index.html`, `src/`, `tests/`, `scripts/`, `docs/`, `play/`)은 손대지 않았습니다.

## 2. 주요 게임 규칙 (구현 기준)

- 고정 시간 스텝 1/60초. 배속은 프레임당 스텝 수만 바꿈(2배속 = 2스텝). 일시정지 = step 미호출 → 적·투사체·대기시간·상태 효과·웨이브·보스 타이머가 함께 멈춤.
- 프레임 dt 는 0.1초로 제한. 백그라운드 복귀 시 밀린 시간을 몰아서 계산하지 않음(자동 일시정지 + 사용자가 "계속하기"를 눌러야 진행).
- 소환: 후보 3개(서로 다른 종류). 배치 성공 시에만 골드 차감·후보 갱신. 선택/취소는 UI 상태라 시뮬레이션에 영향 없음(무료 재추첨 불가). 무료 새로고침 2회 후 12골드.
- 최대 배치 12기(= 각 맵 배치 칸 12개). 가득 차면 합성·판매로 비워야 함.
- 합성: 같은 종류·등급 2기 → +1등급(최대 3). 결과는 "선택한 유닛" 자리, 상대 자리는 비움. 투입 골드는 합산(판매 환급 = 투입 × 60%, 내림 → 합성·판매 반복으로 골드 증가 불가). 대기시간은 둘 중 긴 쪽 유지.
- 이동: 빈칸 이동 또는 자리 교환. 대기시간 유지, 유닛당 1초 이동 대기.
- 타깃: 기본 "가장 앞선 적". 레이저는 광선이 가장 많은 적을 관통하는 방향을 고름(동률이면 앞선 적). 폭탄·기름은 착탄 시점 위치를 예측해 던짐.
- 상태 효과(적): 기름(지속, 점화 시 소모), 화상(더 센 값·긴 시간만 유지, 중첩 없음), 냉각(감속 상한 60%), 빙결(0.6초, 이후 3초 면역), 회오리 표식, 보호막(드론/보스). 지속 피해는 장갑 무시, 직격은 장갑만큼 감소(최소 1).
- 회오리: 경로 위 "앵커"(칸에서 가장 가까운 경로 지점)로 적을 경로를 따라 끌어모음(경로 이탈·순간이동 없음, 다른 구간 적은 제외). 보스는 감속 15% + 표식만.
- 연계 6종: 원본 공격만 연계를 일으키고 연계로 생긴 피해(combo=true)는 다시 연계를 일으키지 않음. 적별 재발동 대기(점화 1.5초, 파편 1초), 회오리별 화염 회오리 5초 대기, 번개는 방문 집합으로 같은 적 재타격 금지.
- 공병: 지원 범위 96/104/112, 공격 속도 +18/26/34%, 두 번째 공병은 50% 반영, 상한 45%. 레이저가 범위 안이면 6/5/4초마다 과충전(×2.2, 더 굵고 긴 광선, 보호막 관통). 3등급 공병은 8초마다 범위 안 유닛 다음 공격 ×1.6.
- 보스: 골렘(9웨이브) 체력 50% 이하 장갑 +4·속도 ×1.35. 코어 마스터(18웨이브) 2초 예고 후 보호막(보통 14초/어려움 10초 주기), 가속(20/15초), 체력 70·35%(어려움 75·50·25%)에서 부하 6기 소환. 보스가 기지에 닿으면 즉시 패배.
- 어려움: 일반 적 체력 ×1.4, 속도 ×1.1, 등장 간격 ×0.85, 지원형·추가 그룹, 보스 패턴 빈도 상승. 보스 체력은 배율의 절반만 적용.
- 저장: 메타(`combo_rush_meta`)와 체크포인트(`combo_rush_run`). 체크포인트는 **각 웨이브의 준비 단계 시작 시점** 전체 상태(맵·난이도·웨이브·체력·골드·유닛·후보·무료 새로고침·시드·난수 상태·runId). 승패 시 삭제. 승패 기록은 runId 별 1회만 반영.

## 3. 밸런스 수치 위치와 변경 기록

모든 수치는 `game/src/data/*.ts`. 봇 시뮬레이션(`npm run balance`)으로 조정했습니다.

| 회차 | 변경 | 이유 |
|---|---|---|
| 초기 | 시작 100골드, 소환 25, 새로고침 무료 2회/12골드, 판매 60%, 웨이브 보상 12+2w, 적 처치 1~7골드 | 한 판 총 골드 ≈ 1,100~1,300 → 3등급 4~7기 목표 |
| 1 | 공병 지원 반경 74 → 96 | 맵의 칸 간격(90)보다 작아 아무 유닛도 지원받지 못함 |
| 1 | 레이저 조준을 "가장 많은 적 관통" 방식으로 변경 | 앞선 적만 조준하면 직선 구간의 관통 이점이 나오지 않음 |
| 2 | 후반 체력 곡선 상향(hpMul: w≥10 +0.9, w≥15 +0.7), 골렘 1500→2200, 코어 3400→4600 | 봇이 모든 전략으로 체력 20 승리 → 후반 압박 부족 |
| 2 | 레이저 피해 9→8, 3등급 폭 20→18; 폭탄병 피해 20→14, 간격 2.4→2.5, 사거리 128→120, 파편 5→4·30% | 단일 조합(레이저·폭탄) 과강 |
| 2 | 어려움 배율 1.3→1.4(체력), 1.08→1.1(속도), 보스는 배율 절반 | 어려움과 보통 차이가 없었음 / 보스만 벽이 되는 것 방지 |
| 3 | 화염 3등급 화상 12→10 | 화염 단일 과강 |

현재 봇 결과(3시드): 혼합 조합 보통 A·B 6/6 승리(6~8분), 어려움 2/3, 번개 단일 1/3, 레이저 단일 2/3(체력 6~9), 폭탄·화염·냉기 단일은 승리하지만 느림(8~12분), 무작위 2/3. 자세한 표는 `game/docs/balance-results.md`.

**남은 밸런스 과제**: 봇은 즉시 판단하는 최적 플레이어라 실제 사람 기준으로는 보통이 더 어렵게 느껴질 수 있고, 반대로 폭탄병·화염봇 단일 조합이 여전히 안정적입니다. 사람 플레이 3~5판 뒤 `hpMul`, `BOSS`, `UNITS.bomber/flame` 를 조정하세요.

## 4. 구현 완료 / 미완료

완료: 1~5단계 요구사항 전체(핵심 전투, 유닛 8종·연계 6종·등급 변화, 소환 후보/새로고침/판매/재배치, 도감, 18웨이브·보스 2종·맵 2개·난이도 2개, 모바일 레이아웃·튜토리얼·음향·타격감·일시정지·백그라운드·저장/이어하기, 자동 테스트·브라우저 e2e·봇 밸런스).

미완료 / 제한:
- 실제 휴대폰(iOS Safari / Android Chrome) 실기 확인은 하지 못했습니다(개발 환경에 기기 없음). 헤드리스 Chromium 모바일 뷰포트로만 확인.
- 배경음악 없음(효과음만). 
- PWA(오프라인 설치) 미포함. 단일 파일 `game/play/index.html` 로 대체.
- 웨이브 중간 상태 저장은 정책상 하지 않음(웨이브 시작 시점 복귀).
- 가로 화면은 동작하지만 세로 화면 기준으로 다듬어져 있습니다.

## 5. 바로 이어갈 때 쓰는 명령

```bash
npm install
npm run dev            # http://localhost:5173  (휴대폰: Network 주소)
npm run typecheck
npm run test:rules     # 규칙·연계·저장 (수 초)
npm test               # + 봇 밸런스 (약 1분)
npm run build && npm run e2e   # 단일 파일 빌드 후 Chromium e2e (스크린샷 game/e2e-out/)
```

디버그 파라미터: `?seed=123`(고정 시드), `?dev=1&speed=8`(개발용 배속, e2e 승리 흐름에 사용). 브라우저 콘솔 `window.__game` 으로 상태(`state`), 액션(`act({type:'early'})`), 뷰(`view`) 접근 가능.

## 6. 현재 남아 있는 문제 / 다음 작업 제안

1. 실기기에서 터치 반응·오디오 언락·FPS 확인 (특히 iOS Safari 의 AudioContext).
2. 사람 플레이 기반 밸런스 재조정(3절 참고). 초반 골드 부족 여부, 12칸 가득 찬 뒤 판매·재소환 흐름이 자연스러운지 확인.
3. 화염 회오리·집속 폭격 등 연계가 사람 플레이에서 얼마나 자주 나오는지 관찰 후 조건 완화/강화.
4. 유닛 정보(사거리·피해)를 후보 카드에서 길게 눌러 미리 보는 기능(현재는 배치 후 유닛 메뉴에서만 확인).
5. 가로 화면 전용 레이아웃 다듬기, 태블릿 크기 확인.
