# 칼퇴 서바이버 — 비주얼·사운드 리디자인 방향서 (v2 "야근 네온")

사용자 요청: **"전체적으로 업스케일링, 타격 모션 이펙트·배경음악·효과음 다 너무 구려 — 간지나게."**
목표는 한 번 보면 "오, 이거 제대로 만든 게임이네" 소리가 나는 선명함·손맛·사운드. 기존 게임 규칙·밸런스·UI 흐름은 그대로 둔다.

## 1. 비주얼 콘셉트: 야근 네온 (Overtime Neon)

해가 진 사무실. 모니터·형광등·네온사인만 켜진 어둑한 공간에서 **빛나는 무기와 타격이 화면을 지배**한다.
- **어둡고 채도 낮은 배경 + 채도 높은 발광 효과.** 바닥·장식은 명도를 낮춰(값 15~30%) 무대 뒤로 물러나고, 투사체·폭발·불꽃·보석은 가산 합성 글로우로 튀어나온다.
- **빛의 위계:** 플레이어 주변 조명(따뜻한 스탠드 빛) > 무기 발광 > 적(림라이트) > 바닥.
- **선명함:** 기기 해상도 그대로(최대 dpr 3) 그린다. 스프라이트는 실제 픽셀 크기로 미리 그리고, 성능이 모자라면 해상도보다 **효과를 먼저** 줄인다.
- **움직임:** 모든 것에 무게감 — 피격 시 찌그러짐(squash)·반동, 이동 시 기울기, 죽을 때 팝.

### 근무지별 무드 (조명·색보정)
| 근무지 | 바닥 | 조명/강조 | 분위기 |
|---|---|---|---|
| office 월요일 사무실 | 어두운 슬레이트 카펫 타일 `#1c2430`/`#212b39`, 옅은 격자 | 청록 네온 `#3de0ff` + 포스트잇 노랑 `#ffd84d`, 책상 스탠드의 따뜻한 빛 웅덩이 | 퇴근 직전 저녁 사무실 |
| crunch 분기 마감 야근 | 짙은 남색 `#121a2e` | 모니터 청록·연두 `#38bdf8`/`#a3ff5c` | 새벽 2시 서버실 |
| dinner 부서 회식 | 짙은 적갈색 나무 바닥 `#261612` | 숯불 주황 `#ff8a3d`, 붉은 조명 | 연기 자욱한 고깃집 |
| holiday 명절 친척집 | 어두운 호박색 장판 `#2a2016` | 등불 금색 `#ffcf5a` + 한지 붉은색 `#e03a3a` | 밤의 한옥 |

### 레이어와 후처리 (렌더 담당)
1. 바닥(타일 텍스처 + 스테이지 소품: 책상·파티션·화분·케이블 등 절차적 그림, 은은한 그림자) → `fx.drawGround`(잉크 자국·그을음)
2. 그림자 → 픽업 → 적 → 플레이어 → 궤도체·드론 → 투사체(잔상/리본 트레일) → 광선·고리 → 적 탄
3. `fx.drawWorld`(파티클·불꽃)
4. **조명 레이어:** 화면을 어둡게 덮고(곱하기) 플레이어 주변·광원 자리만 밝게. 광원 = 플레이어, 폭발·번개(`fx.drawLights`), 보스·엘리트 림라이트, 불타는 적.
5. **블룸:** 1/4 해상도 버퍼(+ 1/8 추가 패스)에 빛나야 하는 것만(투사체·광선·폭발·불꽃·보석·오라 가장자리·`fx.drawGlow`) 그린 뒤 가산 합성으로 확대해 얹는다.
5b. `fx.drawLabels`(피해 숫자·떠오르는 글자 — 조명·블룸 뒤, 월드 UI 앞: 어둠에 묻히지 않게)
6. 비네트(근무지 색) → `fx.drawScreen`(번쩍임·집중선·레터박스·피격 붉은 테두리) → 조이스틱·방향 표시
- 품질 단계: `high`(블룸 2단 + 동적 조명 + 트레일 + 파티클 많이) / `medium`(블룸 1단, 조명 단순, 파티클 60%) / `low`(블룸·조명 없음, 그림자 없음, 파티클 30%). 앱이 자동 사다리로 품질과 해상도를 조절한다(`QUALITY_LADDER` in app.ts).

### 개체
- **적:** 이모지 몸 + 2px 짙은 외곽선 + 근무지 조명색 림라이트 + 접지 그림자 + 화난 눈(유지). 피격: 흰 번쩍임 + 60ms 찌그러짐(가로 1.25·세로 0.8) + 살짝 밀림. 엘리트: 바닥 오라 고리 + 금색 외곽선. 보스: 큰 그림자, 붉은 림라이트 맥동, 명판.
- **플레이어:** 더 크고 또렷한 치비 직장인(외곽선·음영·하이라이트), 이동 방향으로 기울기, 달릴 때 먼지, 발밑 빛.
- **투사체:** 무기 고유색 글로우 + 트레일, 진행 방향 회전. 진화 무기는 더 크고 밝게 + 추가 파티클.
- **픽업:** 보석은 반짝이는 글로우 + 끌려올 때 줄무늬 잔상, 코인은 회전 광택.

## 2. 타격감(손맛) 명세 (VFX 담당)
| 순간 | 연출 |
|---|---|
| 적중 | 적중 방향으로 가산 불꽃 3~6개(흰 심 + 무기색), 작은 충격 고리, 숫자 팝 |
| 치명타 | 별 모양 섬광 + 노랑→주황 큰 숫자(!) 살짝 기울어짐 + 미세 흔들림 |
| 처치(일반) | 흰 팝(원형 섬광) + 적 색 종이 파편 6~10개(회전·중력) + 바닥 잉크 자국(5초 페이드) + 작은 빛 |
| 처치(엘리트) | 중형 폭발 + 금화 튐 + 흔들림 0.35 + 빛 |
| 처치(보스) | 다중 폭발 고리 + 색종이 + 줌 펀치 + 섬광 + 강한 흔들림 |
| 폭발(토너·알람·지뢰·폭탄) | ① 섬광 원 ② 두꺼운 충격파 고리 ③ 불꽃 입자 ④ 연기(어둡고 느리게) ⑤ 파편 ⑥ 그을음 자국 ⑦ 빛 펄스 ⑧ 반경 비례 카메라 킥 |
| 번개(연쇄) | 발광 심 + 곁가지, 마디마다 불꽃, 번쩍 빛 |
| 레벨업 | 플레이어 중심 방사형 빛 + 떠오르는 반짝이 고리 + 스타일 글자 |
| 피격 | 붉은 테두리 펄스(화면), 짧은 색수차 느낌 번쩍임, 넉백 |
| 궁극기 | 흰 섬광 → 화면을 가로지르는 충격파 → 집중선 → 줌 펀치 1.08 |
| 보스 등장 | 레터박스 1.5초 + 붉은 맥동 |
| 피해 숫자 | 굵은 디스플레이 서체(Black Han Sans), 1.8배→1 튀어나옴(오버슈트), 감속하며 떠오름, 병합; 일반 흰색, 치명타 노랑→주황, 1000 이상 더 크게 |

## 3. 사운드 방향 (음악·효과음 담당)
전부 WebAudio 실시간 합성(외부 음원 없음). 코어(`platform/audio.ts`)가 버스·리버브·딜레이·리미터·덕킹을 제공한다.

### 음악 (곡 id → 콘셉트)
| 곡 | 콘셉트 | 템포 | 조성·진행 예 |
|---|---|---|---|
| title | 로파이 시티팝, 로즈 FM 피아노·따뜻한 베이스·브러시 드럼, 테이프 느낌 | 88~92 | Fmaj7–Em7–Dm7–G7sus |
| office | 업비트 신스웨이브 펑크, 4/4 킥, 슬랩 느낌 베이스, 밝은 아르페지오, 코러스 리드 | 118~124 | I–V–vi–IV 변형, 7th 화음 |
| crunch | 다크 신스웨이브(단조), 펄스 베이스, 차가운 패드 | 126 | Am–F–C–G |
| dinner | 회식 트로트·디스코 뽕끼(2박 쿵짝, 브라스 신스, 흥) | 128~132 | 단조 트로트 선율 |
| holiday | 국악 퓨전: 5음계, 가야금 느낌 플럭(Karplus-Strong), 장구 리듬 + 현대 비트 | 112~116 | 평조/계면조 5음계 |
| boss | 공격적, 디스토션 베이스, 스네어 롤, 긴장 반음 | 140 | 반음계 긴장 |
| overtime | 다크 테크노, 미니멀·최면적 | 132 | 단조 한 화음 |

- 강도 0~3 레이어: 0 = 패드·베이스, 1 = +드럼, 2 = +아르페지오/멜로디, 3 = +리드·추가 타악·필인.
- 스팅어: victory(시티팝 팡파르), defeat(슬픈 로즈), levelup(짧은 상승 화음), evolve(에픽 라이저+화음), lunch(종소리), bossIntro(저음 브라스+경보).
- 곡 전환은 마디 경계에서 크로스페이드. 킥에 맞춰 패드·베이스 사이드체인 덕킹.
- 레벨: 음악 RMS −24 ~ −14 dBFS, 피크 ≤ 0.99(클리핑 0) — `scripts/audio-check.mjs`로 확인.

### 효과음
- 여러 겹(트랜지언트 + 몸통 + 꼬리), 음높이 무작위 변주, 화면 좌우 위치로 팬, 큰 소리는 리버브 센드 + 음악 덕킹.
- 무기별 정체성: 볼펜 휙(짧은 바람), 커피 지글, 명함 스치는 소리, 서류철 붕붕, 클립 챙, 번개 지직(FM 잡음), 레이저 웅(지속), 토너 퉁 + 폭발, 종이비행기 슉, 엔터키 딸깍+충격파, 드론 삐빅, 법인카드 칭, 포스트잇 톡, 알람 따르릉.
- 처치 팝은 연속 처치에 따라 음이 조금씩 올라가고(콤보), 보석 획득은 5음계로 올라가는 반짝임.
- 피격 음은 짧고 굵게(저역 쿵 + 로우패스 잡음), UI 클릭은 부드럽게.
- 동시 발음 수 제한(스로틀·보이스 캡)으로 탁해지지 않게.

## 4. UI 방향 (UI 담당)
- 서체: 제목·HUD 숫자·배너·큰 버튼 = **Black Han Sans**, 본문 = **Noto Sans KR**(`ui/fonts.ts`의 DISPLAY_FONT/BODY_FONT; Google Fonts, 오프라인이면 시스템 글꼴).
- HUD: 반투명 유리 패널 + 근무지 강조색 얇은 네온 테두리, 시계 글로우, 경험치바 광택 애니메이션, 무기 슬롯 카드(레벨 점), 보스 체력바 재디자인(해골 아이콘, 흰 '따라오는' 잔상 바) — **무기 슬롯 6개가 한 줄이어도 보스바와 겹치지 않게.** 궁극기 버튼: 원뿔 게이지 링, 준비되면 발광 맥동.
- 모달: 레벨업 카드 등급 테두리(무기 청록·패시브 보라·진화 가능 금색), 순차 등장, 상자 빛줄기, 배너 사선 줄무늬·발광 글자.
- 메뉴: 타이틀 로고 업그레이드(디스플레이 서체 + 네온 글로우), 버튼 광택, 화면 간 일관성.
- **바꾸면 안 되는 것:** e2e/스크린샷 스크립트가 쓰는 클래스·문구(`.choice`, `.ult`, `.pausebtn`, `.lunch-card`, `.chest-stage`, `.result-head`, `.confirm-wrap .btn.primary/.danger`, `.title-grid .btn`, `.screen-head .btn.icon`, `.modal`, 버튼 문구 '출근하기', '다음 → 근무지 선택', '🏃 출근!', '▶ 계속 일하기', '🏳 조퇴하기', '받기!').

## 5. 파일 소유 (각 에이전트는 자기 파일만 수정)
| 담당 | 파일 |
|---|---|
| 음악 | `src/platform/music.ts` |
| 효과음 | `src/platform/sfx.ts` |
| 타격감(VFX) | `src/render/fx.ts`, `src/render/juice.ts` |
| 렌더·아트·업스케일 | `src/render/renderer.ts`, `src/render/sprites.ts`, 새 파일 `src/render/*.ts`(예: post.ts, env.ts), `src/content/stages.ts`의 **palette·decor 필드만** |
| UI | `src/style.css`, `src/ui/hud.ts`, `src/ui/modals.ts`, `src/ui/screens.ts`, `src/ui/dom.ts`, `src/ui/sharecard.ts`, `index.html`(head), `public/icon.svg` |
| (공용·수정 금지) | `src/sim/**`, `src/content/**`(위 예외 제외), `src/ui/app.ts`, `src/ui/input.ts`, `src/ui/fonts.ts`, `src/main.ts`, `src/platform/audio.ts`, `src/platform/save.ts`, `src/meta/**`, `tests/**`, `scripts/**` |
공용 파일 변경이 필요하면 결과 보고의 `requests`에 적는다(통합 담당이 반영).

## 6. 계약(인터페이스) — 바꾸지 말 것
- `Fx` 레이어 훅: `drawGround / drawWorld / drawGlow / drawLights / drawLabels / drawScreen(g, View)`, `zoomPunch()`, `shakeOffset()`, `update(dt)`, `reset()`, `invalidateText()`, `setLow()`, 그리고 juice가 부르는 생성 메서드들(`burst, dmg, boom, bolt, text, addShake, addFlash` — VFX가 새 메서드를 더하는 건 자유). `View`, `Quality` 타입은 fx.ts.
- `juiceEvent(fx, ev, w, {vibrate})`, `juiceDeath(fx)` — app.ts가 부른다.
- `Renderer`: `render(w, dt, joy, steps)`, `resize()`, `viewSize()`, `setQuality(q)`, `dprCap`, `camX/camY`, `live`, `fx`. 렌더러는 `fx.zoomPunch()`/`fx.shakeOffset()`을 적용하고 레이어 훅을 위 순서로 부른다.
- 오디오: `createMusic(core, live): MusicAPI`(start/stop/setTrack/setIntensity/stinger/scheduleRange), `createSfx(core): SfxAPI`(play/onSimEvent), `SFX_NAMES`, `TRACK_IDS`, `STINGER_IDS`. 기존 효과음 이름은 계속 동작해야 한다(새 이름 추가 가능). 오프라인 렌더링(`scheduleRange`)이 실시간과 같은 소리를 내야 한다.
- 스프라이트 캐시는 `setSpriteScale`/`clearSpriteCache`로 무효화된다. 글자를 캐시하는 곳은 `onFontsReady` 이후 다시 그려져야 한다(`clearSpriteCache`, `fx.invalidateText`가 호출됨).

## 7. 성능 예산
- 기준(변경 전, 헤드리스 소프트웨어 렌더 390×844@2x): 무거운 장면(적 ≈160, 무기 6) **39.6fps**.
- 변경 후: `medium` 품질에서 기준의 90% 이상, `high` 품질에서 28fps 이상(헤드리스는 GPU가 없어 실기기보다 훨씬 느림). 메인 스레드 작업(앱 `debugState().work`, ms/프레임)은 무거운 장면에서 평균 9ms 이하.
- 금지: 프레임마다 그라디언트·캔버스 새로 만들기, 적마다 save/restore, 프레임마다 문자열 렌더, 무제한 캐시(크기 제한 필수), 적 수에 비례하는 전면 오버드로.

## 8. 점검 도구
- `node scripts/shots.mjs <html> <outDir>` — 타이틀·4개 근무지 초반·무거운 전투·보스·궁극기·레벨업·상자·일시정지 스크린샷 + 무거운 장면 FPS. **Read 도구로 PNG를 직접 보고 판단할 것.**
- `node scripts/audio-check.mjs <html> [--wav]` — 모든 곡(강도 1·3)·스팅어·효과음 오프라인 렌더 → 피크·RMS·클리핑·무음 비율.
- `node scripts/e2e.mjs` — 휴대폰 뷰포트 3종 실기동(play/index.html 기준).
- 빌드: `npx vite build --outDir <dir>` → `node scripts/singlefile.mjs <dir> <playDir>`.

---

# v3 — 가로 모드 우선 · 공격 모션 · 피격 이펙트/모션

사용자 요청: **"가로모드가 나을듯? 공격모션도 개선해줘 피격이펙트, 모션 개선"**
v2(야근 네온)의 룩·사운드는 유지하고, ① 가로 화면을 기본 경험으로, ② 캐릭터와 무기가 "공격하는 동작"이 눈에 보이게, ③ 맞는 쪽(적·플레이어)의 반응과 이펙트가 묵직하게.

## 10. 가로 모드 우선 (UI 담당)
- **기본은 가로.** `settings.orient`: `'land'`(기본, "가로 고정") | `'auto'`("기기 방향 따라"). 기존 저장본은 `'land'`로 정규화된다(save.ts).
- **회전 모드:** `orient === 'land'` + 터치 위주 기기(`(pointer: coarse)` 또는 maxTouchPoints>0이면서 fine 포인터가 아님) + 뷰포트가 세로(innerHeight > innerWidth)이면 `<html>`에 `rot` 클래스를 달고 `#app`을 **CSS로 시계 방향 90° 회전**해 가로로 보여 준다(폰을 왼쪽으로 눕히면 = 기기 윗변이 왼쪽 → 바로 선다). 회전 잠금이 켜진 폰·세로 고정 인앱 브라우저(카카오톡·Claude 앱 등)에서도 가로로 즐길 수 있게 하는 장치다. 기기를 실제로 돌려 뷰포트가 가로가 되면 회전은 자동으로 풀린다.
  - 변환: `#app { position: fixed; top: 0; left: 0; width: <뷰포트 높이>; height: <뷰포트 너비>; transform-origin: 0 0; transform: rotate(90deg) translateY(-100%); }` (dvh/svh 대신 JS가 잰 px 값을 CSS 변수로).
  - 좌표: 물리 좌표 (cx, cy) → 앱 로컬 좌표 **(lx, ly) = (cy, innerWidth − cx)**. DOM 클릭 판정은 브라우저가 transform을 반영하므로 그대로 되고, 조이스틱처럼 clientX/Y를 직접 쓰는 곳(input.ts)만 변환한다. 공용 헬퍼(예: `src/ui/orient.ts`의 `toLocal(cx, cy)`, `effSize()`)를 둔다.
  - 크기: 렌더러는 `canvas.clientWidth/clientHeight`(레이아웃 크기)로 잰다(적용됨). `window.innerWidth/innerHeight`·`getBoundingClientRect()`로 레이아웃을 판단하는 코드는 유효 크기 헬퍼로 바꾼다.
  - 미디어 쿼리 `orientation`·`max-height` 등은 회전 모드에서 거짓이 되므로, **JS가 `<html>`에 붙이는 클래스(`land`/`port`, `rot`, 짧은 높이 `short` 등)와 CSS 변수 `--vw`/`--vh`(유효 뷰포트 px)** 로 레이아웃을 가른다. `vw/vh/dvh` 단위는 `--vw/--vh` 기반 calc로 바꾼다.
  - 안전 영역(노치): 회전 모드에서 로컬 위 = 물리 오른쪽, 로컬 아래 = 물리 왼쪽, 로컬 왼쪽 = 물리 위, 로컬 오른쪽 = 물리 아래. `--sat/--sar/--sab/--sal` 변수로 매핑해서 쓴다.
  - 회전 모드에서 처음 타이틀이 뜨면 "📱 휴대폰을 옆으로 눕혀 주세요" 안내(회전 애니메이션)를 잠깐 보여 준다. 설정에 "화면 방향: 가로 고정 / 기기 방향 따라" 토글.
- **가로 레이아웃이 기준:** 844×390(아이폰), 740×360(작은 안드로이드), 667×375(SE), 1280×720·1920×1080(데스크톱)에서 모든 화면이 스크롤 없이(목록 화면 제외) 보기 좋게. HUD는 네 모서리(왼쪽 위 시계·레벨·무기 슬롯, 오른쪽 위 처치·코인·일시정지, 가운데 위 보스 체력바, 오른쪽 아래 궁극기), 레벨업 카드 3장 가로 배치, 점심·상자·일시정지·결과 2단, 타이틀(로고 왼쪽·메뉴 오른쪽), 캐릭터·근무지 선택·복지·업적·도감 가로 그리드. 세로 레이아웃(`auto` + 세로 기기, 데스크톱 좁은 창)도 계속 동작해야 한다.
- 카메라: `VIEW_SHORT = 420`(짧은 변 월드 단위) 유지 — 보이는 월드 면적이 세로와 같아 적 소환 거리·밀도(밸런스)가 바뀌지 않는다.
- 가능한 기기에서는 일시정지·설정에 "⛶ 전체화면" 버튼(`document.fullscreenEnabled`일 때만; 안드로이드는 전체화면 뒤 `screen.orientation.lock('landscape')` 시도, 실패는 무시).

## 11. 공격 모션 (렌더 담당: 몸·투사체 동작 / VFX 담당: 발사 섬광·입자)
원칙: **예비 동작(짧게) → 액션(빠르게) → 여운(오버슈트·반동)**, 스쿼시&스트레치, 호(arc), 진행 방향 정렬. 한 화면에 무기 6개가 동시에 돌아가도 산만하지 않게 — 강약(주무기 크게, 잦은 무기는 가볍게).

### 플레이어 공격 자세 (렌더러, `shoot` 이벤트의 `ang`·`w` 사용)
| 아키타입 | 자세 |
|---|---|
| shot·spread·homing·bounce (볼펜·클립·종이비행기·법인카드) | 던지기: 팔을 뒤로 젖혔다(40ms) 겨눈 방향으로 휘두름(80ms), 몸이 그 방향으로 살짝 비틀리고 반대쪽으로 1~2u 반동 |
| boomerang (서류철) | 크게 휘둘러 던지기(몸 회전감), 여운 길게 |
| beam (레이저) | 팔을 뻗어 겨눈 채 유지(광선 지속 동안), 손끝에 충전 빛 |
| chain (단축키 번개) | 손가락 딱 — 손끝 전기 스파크 |
| lob (토너) | 머리 위로 들어 던지기(위로 늘어났다 눌림) |
| nova (엔터키) | 쾅 내려찍기: 몸이 눌렸다(스쿼시) 튀어 오름 |
| strike (알람) | 하늘을 가리킴 |
| mine (포스트잇) | 바닥에 탁 붙이기(몸 숙임) |
| aura·orbit·drone | 자세 없음(오라는 김이 맥동, 궤도체는 켜질 때 "펼치기", 드론은 스스로 조준) |
- 자세는 겹치면 최신 것 하나(최소 간격 약 0.12초), 이동 기울기·걷기와 자연스럽게 섞는다. 바라보는 방향은 이동 방향 유지, 팔만 공격 방향으로.

### 투사체·공격체 동작 (렌더러)
| 무기(진화) | 동작 |
|---|---|
| 볼펜(만년필 폭풍) | 발사 순간 0.6→1 커지며 진행 방향으로 늘어남(스트레치 1.3×0.8) 후 복원, 펜촉이 진행 방향, 잉크 줄 트레일(진화: 잉크 방울·잔상 2겹) |
| 클립 샷건(개틀링) | 부채꼴 발사, 클립이 빠르게 텀블링(회전), 짧은 예광선; 개틀링은 연사 느낌의 가는 예광선 |
| 종이비행기(종이학) | 방향 전환 시 기울어지는 뱅킹, 하얀 비행운 두 줄; 종이학은 날갯짓(세로 스케일 진동)+분홍 꽃잎 |
| 법인카드(블랙카드) | 빠른 회전, 튕길 때(진행 각이 크게 바뀐 순간) 번쩍 "팅" 섬광 |
| 서류철(무한 결재) | 크게 도는 부메랑 + 회전 잔상 2~3장(모션 블러) + 휘어진 궤적, 돌아올 때 종이 펄럭 |
| 명함 회오리(VIP) | 켜질 때 플레이어에게서 반지름까지 "펼쳐지고"(0.2초) 꺼질 때 접힘, 카드 뒤집기(가로 스케일 cos), 혜성 꼬리 |
| 레이저(프레젠테이션 빔) | 충전 섬광 → 굵기가 0에서 확 커지며 뻗음 → 흐르는 에너지 줄무늬·깜빡임 → 가늘어지며 사라짐; 빔 끝 불꽃. 프레젠테이션 빔은 넓은 영사 원뿔+먼지 |
| 단축키 번개(Ctrl+Alt+Del) | 번개는 fx; 렌더러는 손끝 스파크·맞은 적 경련 |
| 토너 폭탄(잉크 대홍수) | 포물선 비행(높이 = 4H·k(1−k), 땅에 그림자가 따라가며 커짐), 카트리지 회전, 착탄 시 찌그러짐 |
| 엔터키(기계식 키보드) | 링이 퍼질 때 바닥 파문 여러 겹 + 키캡 글자 튐(fx) |
| 포스트잇(지뢰밭) | 플레이어 손에서 설치 지점까지 짧은 포물선으로 날아가 "착" 붙음(스쿼시), 무장되면 깜빡임·점선 감지 원 |
| 퇴근 알람(6시 종) | 대상 위 예고 원(줄어드는 카운트다운 링) + 흔들리는 알람 → 하늘에서 떨어짐(가속, 그림자 커짐) → 쾅 |
| 드론(편대) | 로터 회전·이동 방향으로 기울기·대상 조준, 발사체는 길쭉한 레이저 볼트 |
| 커피(카페인) | 김이 도는 고리·거품, 피해 틱마다 맥동 |

### 발사 섬광·입자 (VFX, `shoot` 이벤트)
- 발사 지점(x, y)·방향(ang)에 무기색 총구 섬광/휘익 선/종이 먼지 등 아키타입별 작은 연출. 초당 수십 번 나올 수 있으니 아주 가볍게(스로틀).

## 12. 피격 이펙트·모션
### 적이 맞을 때 (렌더러: 몸 반응 / VFX: 입자·섬광)
| 상황 | 몸 반응(렌더러, `Enemy.flash`·`Enemy.hitAng`·넉백 속도 `vx/vy`) | 이펙트(VFX, `hit` 이벤트의 `ang`·`slot`) |
|---|---|---|
| 일반 적중 | 흰 번쩍 → 무기색/붉은 기 잔광, **타격 방향 축으로** 찌그러짐(방향 쪽 눌림·수직 늘어남) + 그 방향으로 3~4u 밀렸다 스프링 복귀 + 살짝 기울어짐(±0.15rad) | 방향성 불꽃·충격선, 무기별 파편(아래 표) |
| 치명타 | 밀림·기울기 1.6배 + 60ms 자세 정지(개별 히트스톱) | 별 섬광 + 큰 파편 |
| 지속 피해(오라·광선·불) | 찌그러짐 대신 잔떨림(지터) | 약한 틱 입자(스로틀) |
| 넉백이 클 때 | 발밑 먼지 끌림 | — |
| 엘리트·보스 | 외곽 번쩍 + 작은 반응(형체 유지), 보스는 큰 타격에 몸 전체가 휘청 | 금색/붉은 파편 |
| 빙결·기절·화상 | 얼음 막·별 빙글·그을음 | — |
### 적 처치 (죽는 모션)
- **렌더러:** 시체 날리기 — 적 스프라이트가 마지막 타격 방향(`kill.ang`)으로 튕겨 날아가며(120~240u/s, 가짜 높이 z + 그림자) 1~2바퀴 회전, 하얗게 번쩍·납작해지며 0.25~0.45초 뒤 사라짐(끝에 `fx.burst`로 작은 퍼프). 엘리트는 금빛, 보스는 비틀거리다 쓰러짐(쓰러진 뒤 fx 대폭발). 동시에 너무 많으면 상한(약 60~80) — 넘치면 작은 적부터 날리기 생략.
- **VFX:** 처치 지점의 흰 팝·색종이·잉크 자국(v2)은 유지하되, 날아가는 몸과 어울리게(팝은 짧고 강하게).
### 무기별 적중 파편 (VFX)
| 아키타입 | 파편 |
|---|---|
| shot | 가는 관통선 + 잉크 방울(만년필: 잉크 튐) |
| spread | 금속 불꽃(흰·회색) |
| homing | 종이 조각(종이학: 분홍 꽃잎) |
| bounce | 푸른 카드 모서리 불꽃(블랙카드: 보라 반짝) |
| boomerang | 서류 종이가 흩날림(무한 결재: 붉은 '결재' 도장 자국) |
| orbit | 베이는 불꽃(VIP: 금빛) |
| beam | 지지는 불꽃 + 연기 |
| chain | 작은 전기 튐 |
| strike | 묵직한 충격파 + 먼지 고리 + 파편 |
| lob | 잉크 튐(큰 자국) |
| nova | 키캡 글자(ENTER·A·S·D·F…) 튐 |
| mine | 노란 포스트잇 조각 |
| drone | 푸른 레이저 착탄 불꽃 |
| aura | 김·커피 방울(틱 간격) |
| 궁극기(slot 6) | 궁극기 색 섬광 |
### 플레이어가 맞을 때
- **렌더러:** 몸이 붉게→희게 번쩍, 공격한 쪽(`hurt.x/y`) 반대로 4~6u 튕겼다 복귀 + 기울기, 아픈 표정(찡그린 눈) 0.3초, 무적 시간 깜빡임, 발밑 체력바가 흔들리며 줄어든 만큼 흰 잔상.
- **VFX:** 붉은 테두리 맥동·색수차·카메라 킥(v2) + 플레이어 머리 위 땀방울·별, 붉은 "-피해" 숫자(작게).
- 체력 30% 이하: 숨을 헐떡이는 몸짓(빠른 숨 스케일)·땀.

## 13. 이번 라운드 파일 소유
| 담당 | 파일 |
|---|---|
| 모션(렌더) | `src/render/renderer.ts`, `sprites.ts`, `env.ts`, `post.ts`, `color.ts`, 새 파일 `src/render/*.ts`(예: `motion.ts`, `corpses.ts`) |
| 임팩트(VFX) | `src/render/fx.ts`, `src/render/juice.ts` |
| 가로 모드(UI) | `src/style.css`, `src/ui/hud.ts`, `modals.ts`, `screens.ts`, `dom.ts`, `sharecard.ts`, `input.ts`, 새 파일 `src/ui/orient.ts`, `src/ui/app.ts`(방향·크기·설정 관련), `index.html`, `public/*`, `scripts/e2e.mjs`, `scripts/shots.mjs` |
| 공용·수정 금지 | `src/sim/**`, `src/content/**`, `src/platform/**`, `src/meta/**`, `src/main.ts`, `src/ui/fonts.ts`, `tests/**`, `scripts/singlefile.mjs`, `scripts/audio-check.mjs` |

## 14. 계약(v3 추가)
- 이벤트 필드(시뮬레이션이 채움, 연출 전용): `hit {ang, slot}`, `kill {ang, r, slot, face, uid}`, `hurt {x, y}`, `shoot {x, y, ang}`, 적 `Enemy.hitAng`. 슬롯 → 무기: `w.weapons.find(x => x.slot === slot)`(6 = 궁극기, 7 = 기타).
- `Renderer.onEvent(ev, w)`: app이 매 이벤트마다 `juiceEvent` 다음에 부른다(타이틀 데모 포함). 렌더러 쪽 상태(공격 자세·시체·개별 반응)는 여기서 만든다. `Renderer`는 판이 바뀔 때(`fx.reset()`이 불리는 시점) 자기 연출 상태도 비워야 한다 — 새 월드 객체를 감지해서 비운다.
- 렌더러가 부를 수 있는 fx 메서드(시그니처 고정): `burst(x,y,color,n,speed,size,kind,grav)`, `sparkle(x,y,color,n,spread)`, `glint(x,y,color)`, `impact(x,y,r,big)`, `kick(dx,dy)`, `addShake(v,cap)`. VFX는 이들을 유지한다(내부 구현 변경은 자유).
- 가로 모드: 렌더러는 `canvas.clientWidth/clientHeight`만 믿는다. 회전 모드에서도 `Renderer.resize()`/`viewSize()`는 로컬(가로) 크기를 돌려준다.
- 성능(§7 유지): 무거운 장면(b0f777e 기준 high 46fps·work 1.5ms) 대비 high ≥ 90%, work ≤ 9ms. 적 수·타격 수에 비례하는 새 비용은 상한·풀·스로틀 필수. 시체·자세·개별 반응 상태는 객체 풀/배열 재사용.
